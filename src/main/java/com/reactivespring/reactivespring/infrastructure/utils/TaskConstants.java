package com.reactivespring.reactivespring.infrastructure.utils;

public class TaskConstants {
    // URL for the API
    public static final String API_URL = "/api/v1/tasks";
    // URL for the API with the ID of the task
    public static final String API_URL_ID = "/api/v1/tasks/{id}";
    // URL for the API with the name of the task
    public static final String API_URL_NAME = "/api/v1/tasks/name/{name}";
    // URL for the API with the ID of the task and the name of the task
    public static final String API_URL_ID_NAME = "/api/v1/tasks/{id}/{name}";

}
